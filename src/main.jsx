import React from 'react'
import ReactDOM from 'react-dom/client'
import PatternPrototype from '../pattern-proto.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PatternPrototype />
  </React.StrictMode>,
)